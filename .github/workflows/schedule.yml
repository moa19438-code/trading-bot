name: Daily Scan

on:
  schedule:
    - cron: "35 15 * * 1-5"  # 15:35 UTC = 18:35 KSA (أيام الأسبوع)
  workflow_dispatch:

jobs:
  call-run:
    runs-on: ubuntu-latest
    steps:
      - name: Call /run endpoint
        run: |
          curl -sS "https://trading-bot-1-2kl8.onrender.com/scan?key=${{ secrets.RUN_KEY }}"
